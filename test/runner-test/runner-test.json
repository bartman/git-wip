[
    [ "${TOOL}", "${ONE}", "${TWO}", "${THREE}", "${FOUR}" ],
    [ "${TOOL}", "${ONE}----${TWO}}}}}${THREE}{{{{${FOUR}" ],
    [ "${TOOL}", "${ONE}${TWO}${THREE}${FOUR}" ],
    [ "${TOOL}", "ONE", "${TWO}", "${THREE}", "${FOUR}" ],
    [ "${TOOL}", "${ONE}", "TWO", "${THREE}", "${FOUR}" ],
    [ "${TOOL}", "${ONE}", "${TWO}", "${THREE}", "FOUR" ],
    [ "${TOOL}", "ONE", "TWO", "THREE", "FOUR" ]
]
